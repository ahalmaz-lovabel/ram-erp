"use server";

// موديول accounting في مرحلته الحالية للقراءة فقط (كشف الحساب + المستحقات).
// الصفحات (server components) تنادي دوال الخدمة مباشرة — لا server actions بعد.
// عمليات الكتابة (مرتجعات/تسويات/ردّ مبالغ) ستُضاف هنا بـ wrapAction لاحقًا.

export {};
